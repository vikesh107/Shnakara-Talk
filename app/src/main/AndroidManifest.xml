<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shankaratalk">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShankaraTalk">
        <activity android:name=".mainactivity.Chat_Home"
            android:theme="@style/Theme.ShankaraTalk3">
        </activity>
        <activity android:name=".mainactivity.EditProfile" />
        <activity
            android:name=".mainactivity.HomeActivity"
            android:theme="@style/Theme.ShankaraTalk1" />
        <activity android:name=".mainactivity.Login_page" />
        <activity android:name=".mainactivity.Registration_page">

        </activity>
        <activity android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>